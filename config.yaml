data:
  books_directory: "./data/Middlemarch"
  character_file: "./char_alias.json"
  llm_results_dir: "./llm_results"
  graph_artifacts_dir: "./graph_artifacts"

models:
  llm_model: "qwen3:8b"  # Or "qwen3:8b" or whichever you chose
  llm_host: "http://localhost:11434"

processing:
  target_book_filename: "book_2.txt"
  chunk_size_sentences: 12  # The ideal number of sentences per chunk
  chunk_overlap_sentences: 2 # How many sentences to include from the end of the previous chunk

analysis:
  top_n_results: 10